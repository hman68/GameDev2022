using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyBase : MonoBehaviour
{
    /// <summary>
    /// So the original plan was to compartmentalize, so we could mix and match scripts.
    /// This is still the intention as far as I know.
    /// Also /// will bring up this little summary things, which is pretty neat.
    /// </summary>
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
